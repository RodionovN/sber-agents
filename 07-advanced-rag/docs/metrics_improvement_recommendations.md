# Рекомендации по улучшению метрик RAGAS

## Текущие результаты

-   🔴 **Обоснованность (faithfulness)**: nan (проблема с оценкой)
-   🔴 **Релевантность ответа**: 0.000 (критично низкая)
-   🔴 **Правильность ответа**: 0.197 (низкая)
-   🟡 **Похожесть на эталон**: 0.787 (нормально)
-   🔴 **Полнота контекста**: 0.000 (критично низкая)
-   🔴 **Точность поиска**: 0.000 (критично низкая)

## Анализ проблем

### 1. Context Precision/Recall = 0.000

**Проблема**: Система не находит релевантные документы

**Возможные причины**:

-   Недостаточно документов извлекается на этапе retrieval
-   Проблемы с качеством embeddings
-   Неправильная настройка весов ensemble

**Решения**:

-   Увеличить `SEMANTIC_RETRIEVER_K` и `BM25_RETRIEVER_K` до 15-20
-   Увеличить `RERANKER_TOP_K` до 5-7 (больше документов для финального ответа)
-   Проверить качество embeddings модели

### 2. Answer Relevancy = 0.000

**Проблема**: Ответы не релевантны запросам

**Возможные причины**:

-   Недостаточно контекста передается в LLM
-   Проблемы с промптом
-   Слишком мало документов после reranking

**Решения**:

-   Увеличить `RERANKER_TOP_K` до 5-7
-   Проверить промпт `conversation_system.txt`
-   Убедиться, что контекст правильно форматируется

### 3. Answer Correctness = 0.197

**Проблема**: Ответы содержат неточную информацию

**Возможные причины**:

-   LLM генерирует информацию не из контекста
-   Недостаточно релевантного контекста
-   Проблемы с faithfulness (галлюцинации)

**Решения**:

-   Улучшить промпт для строгого следования контексту
-   Увеличить количество документов
-   Использовать более строгие инструкции в промпте

### 4. Faithfulness = nan

**Проблема**: Ошибка при оценке (возможно rate limit)

**Решения**:

-   Увеличить задержки между запросами в evaluation
-   Проверить логи на наличие ошибок API

## Рекомендуемые изменения конфигурации

### Вариант 1: Улучшение retrieval (рекомендуется)

```bash
# Увеличиваем количество документов на этапе retrieval
SEMANTIC_RETRIEVER_K=20
BM25_RETRIEVER_K=20

# Увеличиваем количество документов после reranking
RERANKER_TOP_K=7

# Оптимизируем веса для лучшего баланса
ENSEMBLE_SEMANTIC_WEIGHT=0.6
ENSEMBLE_BM25_WEIGHT=0.4
```

### Вариант 2: Более агрессивный retrieval

```bash
SEMANTIC_RETRIEVER_K=30
BM25_RETRIEVER_K=30
RERANKER_TOP_K=10
ENSEMBLE_SEMANTIC_WEIGHT=0.7
ENSEMBLE_BM25_WEIGHT=0.3
```

## Проверка качества данных

1. **Проверьте индексацию**:

    ```bash
    # В боте выполните команду
    /index_status
    ```

2. **Проверьте количество документов**:

    - Должно быть достаточно документов для ответов на вопросы из датасета

3. **Проверьте качество embeddings**:
    - Убедитесь, что модель embeddings работает корректно
    - Проверьте логи на наличие ошибок при загрузке модели

## Улучшение промптов

Проверьте файлы промптов:

-   `prompts/conversation_system.txt` - должен строго требовать использование только информации из контекста
-   `prompts/query_transform.txt` - должен улучшать поисковые запросы

## Мониторинг

После изменений:

1. Перезапустите бота
2. Выполните переиндексацию: `/index`
3. Запустите evaluation снова
4. Сравните метрики

## Ожидаемые улучшения

После применения рекомендаций ожидается:

-   Context Precision/Recall: 0.3-0.6
-   Answer Relevancy: 0.4-0.7
-   Answer Correctness: 0.5-0.8
-   Answer Similarity: останется ~0.8
